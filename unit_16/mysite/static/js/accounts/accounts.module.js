(function () {
  'use strict';

  angular
    .module('restblog.accounts', [
      'restblog.accounts.controllers',
      'restblog.accounts.services'
    ]);

  angular
    .module('restblog.accounts.controllers', []);

  angular
    .module('restblog.accounts.services', []);
})();
